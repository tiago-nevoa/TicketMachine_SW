class KeyReceiver { // Receives the frame from Keyboard Reader
    // Initializes the class
    fun init() {
    }
    //  Receives a frame and returns the code of a key, if it exists
    fun rcv(): Int {

    }
}